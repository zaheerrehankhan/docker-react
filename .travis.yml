language: generic
sudo: required
services:
        - docker


before_install:
        - docker build -t cookienomisar/docker-react -f Dockerfile.dev .


script:
        - docker run cookienomisar/docker-react npm run test -- --coverage
